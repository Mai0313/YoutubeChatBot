services:
  app:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: prod
    env_file:
      - .env
    command:
      - uv
      - run
      - --no-dev
      - --frozen
      - youtubechatbot
    restart: always
    pull_policy: always
